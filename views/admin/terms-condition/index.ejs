<%- include('../partials/header') %>

<div class="card">
  <div class="card-header">
    Terms Conditions
  </div>

  <div class="card-body">
   <form action="/admin/terms-condition/create" method="POST">

      <div class="mb-3">
        <label class="form-label">Title</label>
        <input
          type="text"
          name="title"
          class="form-control"
          value="<%= typeof terms !== 'undefined' ? terms.title : '' %>"
        >
      </div>

      <div class="mb-3">
        <label class="form-label">Content</label>

        <textarea
          id="editor"
          name="content"
          class="form-control"
          rows="15"
        ><%= typeof terms !== 'undefined' ? terms.content : '' %></textarea>
      </div>

      <div class="form-check mb-3">
        <input
          type="checkbox"
          class="form-check-input"
          name="isActive"
          value="true"
          <%= typeof terms !== 'undefined' && terms.isActive ? 'checked' : '' %>
        >
        <label class="form-check-label">
          Active
        </label>
      </div>

      <button class="btn btn-primary">
        Save Terms Conditions
      </button>

    </form>
  </div>
</div>

<script src="https://cdn.ckeditor.com/4.22.1/standard/ckeditor.js"></script>

<script>
  CKEDITOR.replace('editor');
</script>

<%- include('../partials/footer') %>